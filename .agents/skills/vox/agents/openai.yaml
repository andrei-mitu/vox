interface:
  display_name: "Vox"
  short_description: "Repo-specific patterns and workflows for vox"
  default_prompt: "Use the vox repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true